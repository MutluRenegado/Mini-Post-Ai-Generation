# Platform Rules

The live source of truth is `MASTER_IMAGE_CREATION_RULES.md`.

- Respect aspect ratio, crop, safe zones, and mobile readability.
- Keep the focal subject large enough for the selected platform.
- Reserve text-overlay space only when required.
- Never adapt a platform merely by adding its name to the prompt.
