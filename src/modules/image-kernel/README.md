# Image Kernel

This module is a clean-architecture scaffold for rule-governed image generation. It intentionally avoids fake provider implementations. Wire the provider adapters to the existing Mini Post App provider clients.
