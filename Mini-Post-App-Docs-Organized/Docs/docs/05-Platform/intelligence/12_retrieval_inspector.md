# Admin Retrieval Inspector

Integrated into the admin interface (`src/admin/RetrievalInspector.tsx`).
Allows administrators to enter structured queries, run retrieval, inspect component scores & explanations, compare candidates, and mark reviewer decisions.
